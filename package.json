{
  "name": "forkify",
  "version": "1.0.0",
  "description": "recipes",
  "default": "index.html",
  "scripts": {
    "start": "parcel --port 3000 --dist-dir ./build-dev index.html",
    "build": "parcel build index.html --dist-dir ./build-dist"
  },
  "author": "rc",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.0",
    "parcel": "^2.8.0"
  },
  "dependencies": {
    "core-js": "^3.26.1",
    "fractional": "^1.0.0",
    "regenerator-runtime": "^0.13.11",
    "sass": "^1.56.1"
  }
}
